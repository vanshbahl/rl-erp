[pytest]
testpaths = tests
addopts =
    -v
    --tb=short
    --strict-markers
    --no-header
markers =
    unit: Unit tests targeting a single service or function
    integration: Integration tests spanning multiple services
    security: Security and authorization tests
    performance: Performance and load tests
