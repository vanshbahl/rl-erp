/**
 * ReactBits Animation Components
 * 
 * This directory contains animation components copied from reactbits.dev.
 * We copy only the components we actually use rather than installing the
 * full package, keeping the bundle lean.
 * 
 * Source: https://reactbits.dev
 * 
 * To add a new component:
 * 1. Find the component on reactbits.dev
 * 2. Copy the source code into a new file in this directory
 * 3. Adapt imports to use our project's dependencies
 * 4. Export from this index file
 */
